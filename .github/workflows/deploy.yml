name: Test Deploy on another branch

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, world! Workflow triggered successfully."
